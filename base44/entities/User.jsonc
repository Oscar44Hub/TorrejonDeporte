{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user",
        "arbitro"
      ],
      "default": "user",
      "description": "Rol del usuario: admin, user (delegado), o arbitro"
    }
  }
}